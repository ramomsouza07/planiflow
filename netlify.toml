[build]
  command = "npm run build:client"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA Fallback: guarantees client-side routing works on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
